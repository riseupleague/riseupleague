import { Metadata } from "next";

const WebsiteManagement = (): JSX.Element => {
	return (
		<section>
			<h1>website management page</h1>
		</section>
	);
};

export const metadata: Metadata = {
	title: "Rise Up Admin | Website Management",
};

export default WebsiteManagement;
