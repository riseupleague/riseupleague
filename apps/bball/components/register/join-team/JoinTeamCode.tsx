import { Card, CardHeader, CardTitle } from "@ui/components/card";
import React from "react";

const JoinTeamCode = () => {
	return (
		<Card>
			<CardHeader>
				<CardTitle>Join a team with a code</CardTitle>
			</CardHeader>
		</Card>
	);
};

export default JoinTeamCode;
